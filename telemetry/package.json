{
  "name": "browserr-telemetry",
  "version": "0.0.0",
  "private": true,
  "description": "Cloudflare Worker that counts anonymous Browserr install heartbeats.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260626.1",
    "wrangler": "^4.105.0"
  }
}
